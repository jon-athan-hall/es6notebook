/**
 * Run this command to install the package.
 * npm install -g eslint
 * And then run this command to check the file.
 * eslint path/to/file.js
 */

/**
 * Create an .eslintrc file to control what errors
 * are tested. Put this file in a particular folder, or
 * in the global system folder.
 */

/**
 * This command comes from Airbnb's github page.
 * npm install -g eslint-config-airbnb eslint eslint-plugin-import
 * eslint-plugin-react eslint-plugin-jsx-a11y
 */

/**
 * Hone your own eslintrc file as you build apps. Fix
 * the rules as they make sense for you and your team.
 */
